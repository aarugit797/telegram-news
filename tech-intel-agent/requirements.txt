# Web framework (async responder)
fastapi==0.115.0
uvicorn[standard]==0.30.6
# Required for request.form() in responder/webhook.py - Twilio posts
# application/x-www-form-urlencoded, and Starlette refuses to parse any
# form body without this installed. Missing it meant every webhook call
# would have failed in production, not just in tests.
python-multipart==0.0.9

# Database
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pgvector==0.3.5

# Scheduling
apscheduler==3.10.4
# The IANA timezone database, backing settings.scheduler_timezone
# ("Asia/Kolkata") which every CronTrigger in agents/scheduler.py is
# anchored to. It was already present, but only transitively via
# apscheduler's tzlocal - pinned explicitly because some slim container
# images ship no system tz database, and if that transitive pull ever
# goes away the timezone fails to resolve at STARTUP, taking the whole
# pipeline process down rather than just one job.
tzdata==2026.3

# Redis
redis==5.0.8

# HTTP requests / feed parsing / HTML text extraction for source agents
httpx==0.27.2
feedparser==6.0.11
beautifulsoup4==4.12.3

# LLM + agent orchestration + observability
anthropic==0.34.2
langgraph==0.2.28
# 0.3.15 is the first release exposing langsmith.wrappers.wrap_anthropic,
# which core/llm_client.py imports. Capped below 0.4 by langchain-core
# (pulled in via langgraph==0.2.28), which requires langsmith<0.4.
langsmith==0.3.15

# Embeddings
voyageai==0.2.3

# WhatsApp delivery
twilio==9.3.2

# Web search tool
tavily-python==0.5.0

# Error monitoring
sentry-sdk[fastapi]==2.14.0

# Environment variables + validation
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0