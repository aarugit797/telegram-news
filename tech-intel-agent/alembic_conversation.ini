; Alembic config for the CONVERSATION database ONLY.
;
;   alembic -c alembic_conversation.ini revision --autogenerate -m "..."
;   alembic -c alembic_conversation.ini upgrade head
;
; The News DB has its own config: alembic_news.ini.
; Run from the tech-intel-agent/ directory.

[alembic]
script_location = alembic/conversation
prepend_sys_path = .

; Date-prefixed filenames so the two independent version histories stay
; readable at a glance.
file_template = %%(year)d%%(month).2d%%(day).2d_%%(hour).2d%%(minute).2d_%%(rev)s_%%(slug)s

; Deliberately blank. alembic/conversation/env.py sets this at runtime
; from app.core.config.settings.conversation_database_url, so the real
; credentials live in .env (gitignored) and never in this tracked file.
sqlalchemy.url =

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
